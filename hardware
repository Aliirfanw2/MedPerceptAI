# Example .env profiles — copy values into medPerceptai/.env (not loaded automatically)

# --- CPU laptop (smooth video, no Llama GPU) ---
# ENABLE_REASONING=1
# LLAMA_N_GPU_LAYERS=0
# INFERENCE_EVERY_N_FRAMES=8
# REASONING_INTERVAL_SEC=5

# --- NVIDIA GPU (faster YOLO + optional Llama GPU) ---
# ENABLE_REASONING=1
# LLAMA_N_GPU_LAYERS=20
# INFERENCE_EVERY_N_FRAMES=6
# REASONING_INTERVAL_SEC=5

# Paths (relative to medPerceptai/):
# MONITOR_VIDEO_PATH=media/demo.mp4
# REASONING_MODEL_PATH=model_weights/llama-3-8b-instruct.Q4_K_M.gguf
