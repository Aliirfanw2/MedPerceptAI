# Core stack (install into env311 — see README)
django
python-dotenv
opencv-python
numpy
torch
ultralytics

# Optional Llama — prebuilt wheel ONLY. Do NOT pip install from source (.tar.gz).
# If pip shows "Building wheel for llama-cpp-python", cancel (Ctrl+C).
# llama-cpp-python
